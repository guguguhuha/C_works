#define _CRT_SECURE_NO_WARNINGS
/*1.为鼓励居民节约用水，A市对居民用水按水量阶梯式计价。
计价标准：按每年用水量统计，不超过220立方米的部分按每立方米3.45元收费；
超过220立方米不超过300立方米的部分按每立方米4.83元收费；
超过300立方米的部分按每立方米5.83元收费。
编程对水费进行计算，要求保留两位小数。*/
#include<stdio.h>
 main()
{
	float a, b;
	printf("请输入您的用水量: ");
	scanf("%f", &a);
	if (a <= 220)
		b = a * 3.45;
	else if (a <= 300)
		b = 220 * 3.45 + (a - 220) * 4.83;
	else
		b = 220 * 3.45 + 80 * 4.83 + (a - 300) * 5.83;
	printf("您的水费为%.2f元", b);

}